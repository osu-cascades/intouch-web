<% provide(:title, "Notifications") %>
<h1>Notifications</h1>
<p>
  <%= link_to "Create New Notification", new_notification_path, style: "color:blue" %>
</p>

<div class = "media">
  <div class = "media-body">
    <table class = "table table-hover">
      <thead>
        <tr>
          <th>Title</th>
          <th>From</th>
          <th>Date</th>
          <th>Content</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @notifications.each do |notification| %>
          <tr>
            <td><%= notification.title %></td>
            <td><%= notification.first_name %></td>
            <td><%= notification.date.strftime("%a %b %e, %l:%e %p") %></td>
            <td><%= notification.content %></td>
            <td><%= link_to 'Show', notification, class: 'btn btn-mini'  %></td>
<<<<<<< HEAD
            <td><%= link_to 'Destroy', notification, method: :delete, class: 'btn btn-mini', data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<h2>Sent Notifications</h2>
<div class = "media">
  <div class = "media-body">
    <table class = "table table-hover">
      <thead>
        <tr>
          <th>Title</th>
          <th>From</th>
          <th>Date</th>
          <th>Content</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @notifications.each do |notification| %>
          <tr>
            <td><%= notification.title %></td>
            <td><%= notification.first_name %></td>
            <td><%= notification.date.strftime("%a %b %e, %l:%e %p") %></td>
            <td><%= notification.content %></td>
            <td><%= link_to 'Show', notification, class: 'btn btn-mini'  %></td>
            <td><%= link_to 'Delete', notification, method: :delete, class: 'btn btn-mini', data: { confirm: 'Are you sure?' } %></td>
=======
            <td><%= link_to 'Delete', notification, method: :delete, class: 'btn btn-mini btn-danger', data: { confirm: 'Are you sure?' } %></td>
>>>>>>> 667ba5edcba2c6d7d25eb68b45ee576cd3c797b4
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
