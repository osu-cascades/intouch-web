<% provide(:title, "Users") %>

<h1>Users</h1>

  <p style="padding-left: 5%">
    <%= link_to "Create New User", new_user_path, class: 'btn btn-mini', id: 'link_id'%>
  </p>

<div class = "media">
  <div class = "media-body">
    <table class = "table table-hover">
      <thead>
        <tr>
          <th>First Name</th>
          <th>Last Name</th>
          <th>User Type</th>
          <th>Username</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <% if user.deactivated != true %>
            <td><%= user.first_name%></td>
            <td><%= user.last_name %></td>
            <td><%= user.user_type %></td>
            <td><%= user.username %></td>
            <td><%= link_to 'Edit', edit_user_path(user), class: 'btn btn-mini' %></td>
            <% if current_user != user %>
              <td><%= link_to 'Deactivate', user, method: :delete, class: 'btn btn-mini', data: { confirm: 'Are you sure?' } %></td>
            <% else %>
              <td></td>
            <% end %>
          <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>